# Base URL of the RAG backend (STT -> chunking/retrieval -> generation).
# The frontend POSTs recorded audio to {NEXT_PUBLIC_API_BASE_URL}/api/query
# See lib/types.ts for the exact request/response contract.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
