# CPU-only PyTorch — compatible with Render cloud (no CUDA GPU).
# The E5 embedder automatically selects CPU when cuda is unavailable.

# API and realtime STT transport.
fastapi==0.111.0
pydantic==2.7.1
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
python-multipart==0.0.9
httpx==0.27.0
websockets==12.0

# Frozen bilingual RAG runtime (CPU build).
numpy==1.26.4
torch==2.3.0
sentence-transformers==3.0.0
transformers==4.41.2
qdrant-client==1.9.1

# Remote hf:// utility (pyarrow only needed for local ingestion).
huggingface-hub==0.23.2
